import { FlashyDirective } from './flashy.directive';

describe('FlashyDirective', () => {
  it('should create an instance', () => {
    const directive = new FlashyDirective();
    expect(directive).toBeTruthy();
  });
});
